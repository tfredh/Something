import { Link, NavLink } from "react-router-dom";
import Button from "@mui/material/Button";

interface StickyHeaderProps {}

export default function StickyHeader({}: StickyHeaderProps): JSX.Element {
    return (
        <header>
            <div>Header</div>
        </header>
    );
}
